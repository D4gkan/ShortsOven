# Windows / Python 3.11 (the supported setup target).
# Ollama is a separate desktop service; tone requests use Python's standard library.
numpy>=1.24,<2
opencv-python>=4.8,<4.12
Pillow>=10
paddlepaddle>=3.0,<4
paddleocr>=3.7,<4
qwen-tts==0.1.1
torch==2.7.1
torchaudio==2.7.1
huggingface_hub>=0.24,<1
soundfile>=0.12.1
faster-whisper>=1.0.3,<2
pydub>=0.25.1
symspellpy>=6.7.7

# Compatibility constraints retained for this Windows environment.
# pandas is used by PaddleX; numba/llvmlite by Qwen's librosa dependency.
pandas==2.2.3
numba==0.58.1
llvmlite==0.41.1
